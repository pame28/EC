CREATE TABLE dbo.JsonEstadoCuenta
(
    Id INT IDENTITY(1,1) PRIMARY KEY,
    FechaCorte DATE,
    No_Tarjeta VARCHAR(50),
    JsonData NVARCHAR(MAX),
    FechaRegistro DATETIME DEFAULT GETDATE()
);





INSERT INTO dbo.JsonEstadoCuenta (FechaCorte, No_Tarjeta, JsonData)
SELECT 
    @FechaCorte,
    be.No_Tarjeta,
    CAST((
        -- TU QUERY COMPLETA AQUÍ
    ) AS NVARCHAR(MAX))
FROM dbo.BandejaEstadoCuenta be
WHERE be.Estado = 0;