CREATE INDEX IX_BandejaEstadoCuenta
ON dbo.BandejaEstadoCuenta (Estado, WorkerId, No_Tarjeta);

CREATE INDEX IX_Encabezado
ON dbo.BandejaEncabezado (No_Tarjeta, Fecha_Corte);

CREATE INDEX IX_Detalle
ON dbo.BandejaDetalle (EC_DetalleNo_Cuenta, Fecha_Corte);

CREATE INDEX IX_Financiamientos
ON dbo.BandejaFinanciamientos (NumeroCuenta, FechaCierre);