Es que mira, yo creo que el problema es porque ... 

la BD del microservicio está en un servidor, peroo, el job está montado en otro servidor.


El microservicio consume el job ...


esta dentro de sql server agent -> Jobs

Ahi se encuentra .. 
para acceder a el .. creé un usuario en msdb y le di los permisos, lo ejecuté manualmente con ese usuario y funcionó correctamente ...
entonces parece que fue que no se actualizó la imagen 
